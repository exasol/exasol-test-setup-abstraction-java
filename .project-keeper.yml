sources:
  - type: maven
    path: pom.xml
    modules:
      - maven_central
      - integration_tests
excludes:
  # Terraform setup
  - "E-PK-CORE-18: Outdated content: '.github/workflows/ci-build.yml'"
linkReplacements:
